# New-API Docker Compose Configuration
# 
# Quick Start:
#   1. docker-compose up -d
#   2. Access at http://localhost:3000
#
# Using MySQL instead of PostgreSQL:
#   1. Comment out the postgres service and SQL_DSN line 15
#   2. Uncomment the mysql service and SQL_DSN line 16
#   3. Uncomment mysql in depends_on (line 28)
#   4. Uncomment mysql_data in volumes section (line 64)
#
# ⚠️  IMPORTANT: Change all default passwords before deploying to production!

version: '3.4' # For compatibility with older Docker versions

services:
  new-api:
    image: crpi-xzr81d0490mc3794.cn-shanghai.personal.cr.aliyuncs.com/reputationly/new-api:latest
    container_name: new-api
    restart: always
    command: --log-dir /app/logs
    ports:
      - "${BIND_HOST:-127.0.0.1}:3000:3000"
    volumes:
      - ./data:/data
      - ./logs:/app/logs
    environment:
      - SQL_DSN=${SQL_DSN:-postgresql://root:123456@postgres:5432/new-api} # ⚠️ IMPORTANT: Change the password in production!
#      - SQL_DSN=${SQL_DSN:-root:123456@tcp(mysql:3306)/new-api}  # Point to the mysql service, uncomment if using MySQL
      - REDIS_CONN_STRING=${REDIS_CONN_STRING:-redis://:123456@redis:6379} # ⚠️ IMPORTANT: Change the password in production!
      - TZ=Asia/Shanghai
      - ERROR_LOG_ENABLED=true # 是否启用错误日志记录 (Whether to enable error log recording)
      - BATCH_UPDATE_ENABLED=true  # 是否启用批量更新 (Whether to enable batch update)
      - NODE_NAME=${NODE_NAME:-new-api-node-1}  # 节点名称，用于审计日志中标识节点身份；多节点/容器部署时建议设置 (Node name used in audit logs; recommended when running multiple instances or in containers)
      - STREAMING_TIMEOUT=${STREAMING_TIMEOUT:-120}  # 流模式无响应超时时间，单位秒，默认120秒 (Streaming no-response timeout in seconds, default 120s)
      - TRUSTED_PROXY_CIDR=${TRUSTED_PROXY_CIDR}  # 反向代理子网，用于从X-Forwarded-For取真实IP (Trusted proxy CIDR for real IP extraction)
      - SESSION_SECRET=${SESSION_SECRET}  # 多机部署时设置，必须修改这个随机字符串！！ （multi-node deployment, set this to a random string!!!!!!!）
      # 在线升级（Watchtower）：下面两个值必须跟 watchtower 服务里的 WATCHTOWER_HTTP_API_TOKEN 一致
      - WATCHTOWER_API_TOKEN=${WATCHTOWER_API_TOKEN:-CHANGE_ME_TO_RANDOM_TOKEN}
      - WATCHTOWER_API_URL=http://watchtower:8080
      - KYC_ENCRYPT_KEY=${KYC_ENCRYPT_KEY}  # 必须在 .env.test / .env.prod 中设置，否则重启后历史 KYC 数据将无法解密
      - KYC_HASH_KEY=${KYC_HASH_KEY}
      # 内容审核原文留存的加密 key（AES-256-GCM，64 位 hex）。
      # 不设置时审核照常运行，但被拦内容一律不留原文——且只对配置之后发生的拦截生效，
      # 事后补配救不回已经丢掉的内容。配没配、配错没配错，看「运营设置 → 内容审核」顶部提示。
      - MODERATION_ENCRYPT_KEY=${MODERATION_ENCRYPT_KEY}
#      - SYNC_FREQUENCY=60  # Uncomment if regular database syncing is needed
#      - GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX  # Google Analytics 的测量 ID (Google Analytics Measurement ID G-FLN7WD8JC9)
#      - UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  # Umami 网站 ID (Umami Website ID a8f20661-db79-43d8-876c-f4743e8b4c5a)
#      - UMAMI_SCRIPT_URL=https://analytics.umami.is/script.js  # Umami 脚本 URL，默认为官方地址 (Umami Script URL, defaults to official URL)

    depends_on:
      - redis
#      - mysql  # Uncomment if using MySQL
    networks:
      - new-api-network
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:3000/api/status | grep -o '\"success\":\\s*true' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: crpi-xzr81d0490mc3794.cn-shanghai.personal.cr.aliyuncs.com/reputationly/redis:latest
    container_name: redis
    restart: always
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:-123456}"]  # ⚠️ IMPORTANT: Change this password in production!
    networks:
      - new-api-network

  postgres:
    profiles: [local]  # 生产环境不启动，本地测试用 --profile local 激活
    image: crpi-xzr81d0490mc3794.cn-shanghai.personal.cr.aliyuncs.com/reputationly/postgres:15
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-123456}  # ⚠️ IMPORTANT: Change this password in production!
      POSTGRES_DB: new-api
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - new-api-network
#    ports:
#      - "5432:5432"  # Uncomment if you need to access PostgreSQL from outside Docker

  # 在线升级服务（API-only 模式，不自动轮询）
  # 在 Web UI 的「设置 → 其他设置 → 检查更新」点「立即安装」时调用
  watchtower:
    image: crpi-xzr81d0490mc3794.cn-shanghai.personal.cr.aliyuncs.com/reputationly/watchtower:latest
    container_name: watchtower
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/.docker/config.json:/config.json   # ACR 登录凭证，用于拉私有镜像
    environment:
      - WATCHTOWER_HTTP_API_UPDATE=true
      - WATCHTOWER_HTTP_API_TOKEN=${WATCHTOWER_API_TOKEN:-CHANGE_ME_TO_RANDOM_TOKEN}  # ⚠️ 必须跟 new-api 服务的 WATCHTOWER_API_TOKEN 一致
      - WATCHTOWER_HTTP_API_PERIODIC_POLLS=false  # 禁用自动轮询，只接受 API 调用
      - WATCHTOWER_CLEANUP=true
      - DOCKER_API_VERSION=1.44  # 避免与新版 Docker daemon 的 API 版本协商失败
    networks:
      - new-api-network

#  mysql:
#    image: mysql:8.2
#    container_name: mysql
#    restart: always
#    environment:
#      MYSQL_ROOT_PASSWORD: 123456  # ⚠️ IMPORTANT: Change this password in production!
#      MYSQL_DATABASE: new-api
#    volumes:
#      - mysql_data:/var/lib/mysql
#    networks:
#      - new-api-network
#    ports:
#      - "3306:3306"  # Uncomment if you need to access MySQL from outside Docker

volumes:
  pg_data:
#  mysql_data:

networks:
  new-api-network:
    driver: bridge
