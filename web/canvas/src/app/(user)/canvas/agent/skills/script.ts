export const SCRIPT_SKILL = `
【剧本与镜头拆解 Skill】

## 阶段

创作从文字开始，文字确认后才动媒体。文字节点不消耗生成额度，用户改起来最快。

1. **方向**：用户给出模糊想法时，先给 3-5 个方向或叙事节拍（每条一句话），等用户挑。不要在这一步写完整剧本。
2. **剧本/文案**：方向定了再展开。落成一个文本节点，标题写清是什么（"主剧本""30s 广告文案"）。
3. **拆镜头**：用 canvas_create_text_nodes 一次性建出每个镜头的卡片。
4. **提取锚点**：从镜头表里拎出需要预先做出来的角色、产品、场景、声音，各建一个文本节点说明需求。

## 镜头卡内容

每张镜头卡固定写这几项，缺项留空但不要省略字段名：

- 镜号与时长（时长必须是视频能力 durations 里的合法值）
- 画面：主体、动作、环境、景别
- 镜头运动：固定/推/拉/摇/跟/环绕
- 声音：对白原文 / 旁白原文 / 音效 / 音乐，没有就写"无"
- 依赖：这一镜需要哪些角色图、场景图、音频

## 结构

- 主剧本 → 各镜头卡，一对多连线（镜头确实是从剧本拆出来的）。
- 镜头卡之间**不连线**，它们是平级的。
- 锚点需求节点连到主剧本或对应镜头，看它是全片通用还是单镜专用。

## 时长

- 用户给了总时长就按它分配，每镜时长之和不超过总时长。
- 每镜时长必须落在视频模型支持的 durations 上；先 canvas_list_capabilities 查，不要拆出 7 秒这种不被支持的镜头再回头改。
- 一个 5 秒镜头只能承载一个动作。信息塞不下就拆成两镜，不要压缩。

## 改写

- 局部改用 canvas_update_node_text 原地改。
- 结构性重来（换叙事方式、换主线）时建新节点并连线，保留旧版供对比，不要直接覆盖。
`.trim();

export const ORGANIZE_SKILL = `
【整理 Skill】

用户说"画布太乱""帮我整理""排一下"。

## 做法

直接调 **canvas_arrange_nodes**。它按连线拓扑分层，横向是管线阶段（上游在左、下游在右），纵向排开同层的并列节点，同层内沿用当前次序所以不会把用户已有的排列打乱。

- 整理全部：不传 nodeIds。
- 只整理一部分：传 nodeIds。
- 需要竖着排：direction 传 "vertical"。

**不要**自己算坐标再调 canvas_move_nodes——你看不到节点真实尺寸和全局布局，算出来会重叠。canvas_move_nodes 只用于用户明确要求把某个节点挪到某处这种单点操作。

## 分组

**canvas_create_group** 把若干节点框进一个分组，用来表达"同属一个角色/场景/章节"。

- 分组**不参与生成、不传递输入**。生产依赖一律用连线，不要为了表示归属去连线。
- 典型用法：一组角色设定表（基准图 + 各变体）一个分组；一幕的所有镜头一个分组。
- 归属是几何包含：分组框会自动包住这批节点，之后谁被拖进框里谁就入组，拖出去就出组。
- 分组不参与拓扑排版；canvas_arrange_nodes 排完成员后会自动把分组框重新包上去。

## 边界

- 整理只改位置。**不要**顺手删节点、改标题或改连线。
- 没有连线的孤立节点会被排到第 0 层，这是预期行为。用户嫌乱可以建议他先把该连的连上。
`.trim();
