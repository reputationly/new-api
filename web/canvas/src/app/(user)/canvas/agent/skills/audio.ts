export const AUDIO_SKILL = `
【声音 Skill】

## 能力选择

**语音**

- **tts_synth 语音合成**：最常用。输入只有要念的文字；用 metadata.speaker 选预设音色（vivian / ryan / aiden / serena / dylan / eric / ono_anna / sohee / uncle_fu），metadata.language 可选口音（含北京话、四川话等方言）。没有特殊要求就用它。
- **tts_design 声音设计**：不挑现成音色，用文字描述想要的声线。metadata.instructions 必填，写"温柔女声，语速平缓，带一点笑意"这类描述。
- **tts_emotion 情感合成**：要指定情绪强度时用。必须挂一段**参考音色**音频（metadata.voice），可选再挂一段情感参考音；emotion 从喜/怒/哀/惧/厌恶/低落/惊喜/平静里选，emoAlpha 调强度（默认 0.65）。
- **tts_dialogue 双人对话**：一次合成两个人的对白。提示词写成 [S1]…[S2]… 的脚本格式，并且必须挂两段参考音（说话人1、说话人2 各一段），用 sourceNodeIds 顺序或槽位绑定区分，**顺序错了会串音**。

**音乐与音效**

- **t2m 文生音乐**：从文字生成整曲。可填歌词（留空自动生成）、时长、人声语种、BPM。
- **cover 音乐改编**：挂一段参考音频，改编风格。
- **repaint 音乐重绘**：挂源音频，重绘。
- **svs 歌声合成**：要挂两段音频——音色参考人声 + 目标曲/伴奏。
- **t2a 文生音效**：短音效，默认 10 秒。

给已有视频配声音的能力在**视频**大类里，不在这里，见下。

## 写法

- 语音类能力的 prompt 是**实际要念出来的文字**，不是对声音的描述。想描述声线请用 tts_design 的 instructions 或 tts_emotion 的参考音。
- 念白里不要写舞台提示（"（停顿）""（叹气）"），那些会被读出来。需要停顿就用标点和分句。
- 数字、英文缩写按希望念出的方式写（"2026 年"写成"二〇二六年"更稳）。

## 与视频的关系

- 数字人（s2v）需要先有一段语音：先用 tts_synth 生成音频节点，等它 success，再建 s2v 节点同时挂人物图和这段音频。
- 给已有视频配声音用 **v2a 视频配音**（在视频大类里）。注意它的**产物是配好音的视频，不是音频文件**——不要指望拿到一个 audio 节点。
- 视频能力本身是否带原生声音取决于模型，不要声称生成的视频一定有音轨。
`.trim();
